$!
$$
Inside multiline comment !$

function_declarations:
function (dataset ds)->my_func(dataset ds1, dataset ds2, integer i)->dataset{
    
    ds = ds1;
    array(integer) arr = {1, 2, 3};
    ds->col(0) *= 10;
    (ds1,ds2)->join({
        integer i;
    })-> row () -> show ();
    string st = "string'f";
    return ds1;
}
start:
dataset ds = read("ds", "csv"); $$ should be double quoted
dataset ds1;
ds1->my_func(ds1, ds1, 0);
integer i = 10;
ds->col(i)->show_bar();
$!
invalid comment !$