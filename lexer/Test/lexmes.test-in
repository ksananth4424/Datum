$$ lexer test file for lexmes

integer first_int;
float first_float;
char first_char;
string first_string;
bool first_bool;
dataset first_dataset;
array first_array;

integer second_int;
float second_float;
char second_char;
string second_string;
bool second_bool;
dataset second_dataset;
array second_array;

$$ second single-line comment

$! first
multi-line
comment
!$

	$$     third single-line comment

$$ second single-line comment
$! first
multi-line
comment
!$

	$$ fourth single-line comment

	$! second multi-line comment !$

$$ $! first multi-line comment inside a single-line comment !$

$! $$ first single-line comment inside a multi-line comment
$$ second single-line comment inside a multi-line comment !$

first_int++;
first_int--;
first_int = first_int + second_int;
first_int = first_int - second_int;
first_int = first_int * second_int;
first_int = first_int / second_int;
first_int = first_int % second_int;
first_int =  second_int;
first_float *= second_float;
first_float /= second_float;
first_float %= second_float;
first_float += second_float;
first_float -= second_float;


if(first_int > second_int)
{
	first_int = second_int;
}
if(first_int < second_int)
{
        first_int = second_int;
}
if(first_int >= second_int)
{
        first_int = second_int;
}
if(first_int <= second_int)
{
        first_int = second_int;
}
if(first_int == second_int)
{
        first_int = second_int;
}
if(first_int != second_int)
{
        first_int = second_int;
}

if(first_int and second_int)
{
        first_int = second_int;
}
if(first_int or second_int)
{
        first_int = second_int;
}